import React, { Component } from 'react';

import '../css/Slider.sass';

class Slider extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <div className="slider">
        slider component in development
      </div>
    );
  }
}

export default Slider;
